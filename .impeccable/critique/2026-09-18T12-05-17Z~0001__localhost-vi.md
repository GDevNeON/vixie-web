---
target: /vi/ light+dark gate
total_score: 30
max_score: 32
na_heuristics: 7,10
p0_count: 0
p1_count: 0
target_identity: "url:http://localhost:4321/vi"
timestamp: 2026-09-18T12-05-17Z
slug: localhost-vi
---
⚠️ DEGRADED: single-context (no sub-agent/Task tool exposed in this session — Assessment A and B run sequentially in-thread)
Method: degraded single-context · Phase 2 Task 2-6 quality gate · target http://localhost:4321/vi/ · dark · mode Persuade

# Critique — `/vi/` dark (1440×900, preview :4321 build 2026-09-18)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | solid — same live states, verified on ink |
| 2 | Match System / Real World | 4 | solid — natural VI copy on dark, no legibility loss |
| 3 | User Control and Freedom | 4 | solid — Esc+focus-return, same-path swap, fallbacks |
| 4 | Consistency and Standards | 4 | solid — dark token swap identical to EN path |
| 5 | Error Prevention | 4 | solid — same constraints |
| 6 | Recognition Rather Than Recall | 3 | theme toggle icon-only (sun shown), same as all surfaces |
| 7 | Flexibility and Efficiency | n/a | Persuade static shell — no repeated task to accelerate |
| 8 | Aesthetic and Minimalist Design | 3 | same tight diacritic clearance + strip void; ink surfaces coherent |
| 9 | Error Recovery | 4 | solid — VI 404 dark axe runs clean (16-run matrix) |
| 10 | Help and Documentation | n/a | Persuade shell — help IA deferred to Ph6 docs |
| **Total** | | **30/32** | **Excellent (93.8%)** |

## Design Specificity Verdict

**LLM assessment**: VI-dark is the most demanding combination (stacked diacritics + ink surfaces + vivid accents) and it holds — paper H1 with cyan `sống` on ink is crisp, dead chips re-tokenized in light-muted stay readable, footer columns and giant `vixie.` mirror the light composition exactly. No locale×theme interaction defects.
**Deterministic scan**: `impeccable detect --json src/` → exit 0, zero findings.
**Visual evidence**: `.impeccable/critique/surface-vi-dark.png` (html class `js js-gated dark lenis`, zero console/page errors). Screenshots+console are the fallback signal.

## Overall Impression

The hardest surface passes like the easiest one — that is the quality ratchet working. Same three logged notes, nothing new.

## What's Working

1. **No locale×theme interaction defects** — diacritics, cyan accent, dashed chips, hairlines all correct simultaneously.
2. **Symmetric dark system** — identical swap path as EN (shared tokens, no per-locale overrides).
3. **Axe dark coverage** — VI pages included in the 16-run matrix (V8), not just EN.

## Priority Issues

- **[P2] H1 stacked-mark clearance tight**: same as VI light; owner Phase 3. *Suggested command*: `$impeccable typeset`
- **[P2] Sample strip empty volume**: same as all surfaces; owner Phase 3/4. *Suggested command*: `$impeccable polish`
- **[P3] Icon-only theme toggle**: same as all surfaces; accepted per D11 lock. *Suggested command*: `$impeccable clarify`

## Persona Red Flags

**Sam**: none — VI dark axe runs clean; focus ring + skip-link verified on ink; `aria-pressed`/`aria-current` announce correctly (dict-driven labels).
**Jordan**: none — same clarity as VI light.
**Casey**: none — same targets/behavior; theme persists.

## Minor Observations

- OS-dark → `.dark` pre-paint path used for capture; no flash; matches V6 e2e mechanism.
- Nothing dark-specific to fix; the P2/P3 set is identical across all four surfaces (systematic, owned, accepted).

## Cognitive Load / Emotional Journey

8/8 pass, same as siblings. The four surfaces are cognitively one surface in two languages and two lights — which is exactly the Phase 2 contract.

Questions skipped: automated quality-gate run (Task 2-6) — no operator present; P2/P3 findings logged with owner phases in `.planning/phase-02-baseline.md`.
